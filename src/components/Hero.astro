---
import { Icon } from 'astro-icon/components'
import { PATH, PATH_CURSADA } from '../../paths.js'
const { src = `${PATH}/UNAHUR-LOGO.png` } = Astro.props
---

<section class="hero my-24">
  <div class="container">
    <div class="grid grid-cols-1 items-center gap-24 lg:grid-cols-2">
      <div class="flex flex-col items-center gap-8 md:items-start">
        <h1 class="text-center text-4xl md:text-6xl lg:text-8xl">
          <span class="text-gradient">Programación con Objetos I</span>
        </h1>
        <div class="min-[500px] flex flex-col justify-center gap-3 md:flex-row md:flex-wrap">
          <a class="button has-icon" href={PATH_CURSADA}>
            <Icon name="ion:star-outline" />
            Cursada
          </a>
          <a class="button has-icon color-secondary" href="">
            <Icon name="ion:bookmark-outline" />
            Cronograma
          </a>
        </div>
      </div>
      <img class="ml-40 hidden lg:block" src={src} alt="" decoding="async" />
    </div>
  </div>

  <style lang="scss">
    .text-gradient {
      background: linear-gradient(315deg, var(--primary-200) 25%, var(--secondary-500));
      background-clip: border-box;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    :global(.darkmode .text-gradient) {
      background: linear-gradient(315deg, var(--primary-200) 25%, var(--secondary-400));
      background-clip: border-box;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
  </style>
</section>
